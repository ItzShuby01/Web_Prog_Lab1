# Basic server settings
ServerRoot "/usr/local/apache2"
Listen 80
ServerName localhost

# Load essential modules
LoadModule mpm_event_module modules/mod_mpm_event.so
LoadModule unixd_module modules/mod_unixd.so
LoadModule dir_module modules/mod_dir.so
LoadModule alias_module modules/mod_alias.so
LoadModule proxy_module modules/mod_proxy.so
LoadModule proxy_fcgi_module modules/mod_proxy_fcgi.so
LoadModule log_config_module modules/mod_log_config.so
LoadModule headers_module modules/mod_headers.so
LoadModule mime_module modules/mod_mime.so
LoadModule authz_core_module modules/mod_authz_core.so
LoadModule authz_host_module modules/mod_authz_host.so

# User & group
<IfModule unixd_module>
    User www-data
    Group www-data
</IfModule>

# Document root
DocumentRoot "/var/www"
<Directory "/var/www">
    Options Indexes FollowSymLinks
    AllowOverride None
    Require all granted
</Directory>

# Directory index
<IfModule dir_module>
    DirectoryIndex index.html
</IfModule>

# Logging to stdout/stderr for Docker
ErrorLog /proc/self/fd/2
LogLevel warn
CustomLog /proc/self/fd/1 combined

# Security: deny access to .ht* files
<Files ".ht*">
    Require all denied
</Files>

# CGI-bin (optional)
<IfModule alias_module>
    ScriptAlias /cgi-bin/ "/usr/local/apache2/cgi-bin/"
</IfModule>

<Directory "/usr/local/apache2/cgi-bin">
    AllowOverride None
    Options None
    Require all granted
</Directory>

# Proxy requests for /calculate to the FastCGI service.
# This pattern is the most reliable for passing query strings.
ProxyPassMatch "^/calculate(.*)$" "fcgi://fcgi:1447$1"

# remove headers that can interfere
<IfModule headers_module>
    RequestHeader unset Proxy early
</IfModule>

# MIME types
<IfModule mime_module>
    TypesConfig conf/mime.types
    AddType application/x-compress .Z
    AddType application/x-gzip .gz .tgz
</IfModule>
